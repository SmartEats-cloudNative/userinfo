server:
  port: 9092

spring:
  profiles:
    active: dev
  application:
    name: UserDetails
  datasource:


    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect   # <-- valid for Hibernate 6
    # or: org.hibernate.dialect.MySQL8Dialect




